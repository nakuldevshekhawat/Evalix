{
  "name": "srms",
  "version": "2.0.0",
  "description": "Student Result Management System — Premium Web App",
  "private": true,
  "scripts": {
    "install:all": "cd webapp/api && npm install && cd ../client && npm install",
    "dev:api":    "cd webapp/api && node index.js",
    "dev:client": "cd webapp/client && npm run dev",
    "build":      "cd webapp/client && npm install && npm run build"
  }
}
