{
  "name": "evailx",
  "version": "2.0.0",
  "description": "Evailx — Premium Web App",
  "private": true,
  "scripts": {
    "install:all": "cd webapp/api && npm install && cd ../client && npm install",
    "dev:api": "cd webapp/api && node index.js",
    "dev:client": "cd webapp/client && npm run dev",
    "build": "npm run install:all && cd webapp/client && npm run build",
    "start": "cd webapp/api && node index.js"
  }
}
